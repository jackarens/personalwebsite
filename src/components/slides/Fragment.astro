---
interface Props {
	class?: string;
	as?: "span" | "li" | "div";
}

const { class: className, as: Element = "span" } = Astro.props;
---

<Element class:list={["fragment", className]}>
	<slot />
</Element>
